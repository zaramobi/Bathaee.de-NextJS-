FROM node:22-alpine

WORKDIR /app

# Install dependencies once; source is mounted at runtime so no COPY needed
COPY package.json package-lock.json* ./
RUN npm ci

EXPOSE 3000
# CMD is overridden by docker-compose.dev.yml to `npm run dev`
CMD ["npm", "run", "dev"]
